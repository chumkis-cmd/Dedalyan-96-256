# Основная реализация (dedalyan.py) и CLI работают на чистой стандартной
# библиотеке -- ничего из перечисленного для них не нужно.
#
# Зависимости ниже нужны для тестов, криптоанализа и демонстрации.

# Тесты и криптоанализ: массивы и агрегирование результатов.
numpy>=1.22

# Точные p-значения (неполная гамма-функция) в статистических тестах.
# Без scipy есть запасной путь -- приближение Уилсона--Хилферти, но оно
# заметно грубее на малых степенях свободы.
scipy>=1.8

# Argon2id для demo.py и веб-интерфейса. Только для них: сам шифр
# от argon2 не зависит.
argon2-cffi>=21.3
